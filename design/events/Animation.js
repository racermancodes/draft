class Animation {
    constructor(obj, data) {
        this.object3d = obj;
        this.data = data;
    }
    animate() {
        if (this.data == undefined)
            return;
    }
}
export { Animation };
